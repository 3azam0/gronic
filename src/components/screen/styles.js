import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  innerFixed: {
    flex: 1,
    justifyContent: "flex-start",
    alignItems: "stretch",
  },
  innerScroll: {
    justifyContent: "flex-start",
    alignItems: "stretch",
  },
});
